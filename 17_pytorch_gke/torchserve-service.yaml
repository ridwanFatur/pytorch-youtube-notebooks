apiVersion: v1
kind: Service
metadata:
  name: torchserve
  namespace: torchserve
spec:
  type: LoadBalancer
  selector:
    app: torchserve
  ports:
  - name: inference
    port: 80
    targetPort: 8080
  - name: management
    port: 8081
    targetPort: 8081